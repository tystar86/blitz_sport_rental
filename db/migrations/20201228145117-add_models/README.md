# Migration `20201228145117-add_models`

This migration has been generated by tystar at 12/28/2020, 3:51:17 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "Item" (
"id" SERIAL,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "EAN" TEXT NOT NULL,
    "model" TEXT NOT NULL,
    "modelYear" INTEGER,
    "brand" TEXT NOT NULL,
    "size" TEXT NOT NULL,
    "quantity" INTEGER NOT NULL,
    "invoices" TEXT,
    "purchasePrice" DECIMAL(65,30),
    "sellingPrice" DECIMAL(65,30),
    "sellId" INTEGER,
    "categoryId" INTEGER NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "Customer" (
"id" SERIAL,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "firstName" TEXT NOT NULL,
    "lastName" TEXT NOT NULL,
    "personalID" TEXT NOT NULL,
    "phoneNumber" INTEGER NOT NULL,
    "email" TEXT NOT NULL,
    "address" TEXT NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "Category" (
"id" SERIAL,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "name" TEXT NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "Sell" (
"id" SERIAL,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "returnedAt" TIMESTAMP(3),
    "customerId" INTEGER,

    PRIMARY KEY ("id")
)

CREATE UNIQUE INDEX "Item.EAN_unique" ON "Item"("EAN")

CREATE UNIQUE INDEX "Customer.personalID_unique" ON "Customer"("personalID")

CREATE UNIQUE INDEX "Category.name_unique" ON "Category"("name")

ALTER TABLE "Item" ADD FOREIGN KEY("categoryId")REFERENCES "Category"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "Item" ADD FOREIGN KEY("sellId")REFERENCES "Sell"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "Sell" ADD FOREIGN KEY("customerId")REFERENCES "Customer"("id") ON DELETE SET NULL ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201228142751-initial..20201228145117-add_models
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -40,4 +40,48 @@
   antiCSRFToken      String?
   publicData         String?
   privateData        String?
 }
+
+model Item {
+  id            Int      @default(autoincrement()) @id
+  createdAt     DateTime @default(now())
+  updatedAt     DateTime @updatedAt
+  EAN           String   @unique
+  model         String
+  modelYear     Int?
+  brand         String
+  size          String
+  quantity      Int
+  invoices      String?
+  purchasePrice Float?
+  sellingPrice  Float?
+  category      Category
+}
+
+model Customer {
+  id        Int      @default(autoincrement()) @id
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  firstName      String
+  lastName       String
+  personalID     String    @unique
+  phoneNumber    Int
+  email          String
+  address        String
+}
+
+model Category {
+  id        Int      @default(autoincrement()) @id
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  name      String   @unique
+}
+
+model Sell {
+  id        Int      @default(autoincrement()) @id
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  returnedAt    DateTime?
+  items         Item[]
+  customer      Customer?
+}
```


